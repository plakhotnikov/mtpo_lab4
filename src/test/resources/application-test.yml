spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
  data:
    rest:
      base-path: /api

app:
  idempotency:
    enabled: true
    key-ttl-hours: 24
